import 'package:flutter/material.dart';

class BoardBackgroundComponent extends StatelessWidget {
  const BoardBackgroundComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
